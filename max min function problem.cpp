#include<stdio.h>
int maximum(int num);
int minimum(int num);
int main()
{
	int a,b,max,min,sum,num;
	printf("enter any two numbers");
    scanf("%d%d",&a,&b);
    if(a<b)
    {
    if(a<b)
    printf("minimum=a");
    else
    printf("minimum=b");
     
  sum=minimum(num);
  
}
      else(a<b);
    {
    if(b>a)
    printf("maximum=a");
    else
    printf("maximum=b");
    sum=maximum(num);
    }
    
    
    return 0;
}

int maxmum(int num)
{
  int a,b;
  return a,b;
}
int minimum(int num)
{
  int a,b;
  return a,b;
}
